[package]
name = "shell-quote"
description = "A Rust library for shell quoting strings, e.g. for interpolating into a Bash script. This is not as simple as most people think!"
version = "0.1.0"
authors = ["Gavin Panella <gavinpanella@gmail.com>"]
license = "Apache-2.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
